# Simple Chat Application (Python Socket Programming)

This is a beginner-friendly client–server chat application built using Python’s `socket` and `threading` modules.

## Features
- Multiple clients can connect.
- Nickname support.
- Broadcast system: each message is delivered to all connected users.
- Server shows join/leave logs.
- Runs locally on Windows CMD.

